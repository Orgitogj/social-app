import { Text, View } from "react-native";

const index =()=>{
  const router=useR
}
  return (
    <View>
    <Button title="welcome" onPress={()=> router.push('welcome')}></Button>
    </View>
  );
}
