import { View, Text, Pressable } from 'react-native'
import React from 'react'

const BackButton = ( size=) => {
  return (
    <Pressable>
     <Icon name="arrowLeft" strokeWidth={2.5}  size={}
    </Pressable>
  )
}

export default BackButton