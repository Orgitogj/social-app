import { View, Text } from 'react-native'
import React from 'react'

const Loading = ({size="large",color=theme.}) => {
  return (
    <View>
      <Text>Loading</Text>
    </View>
  )
}

export default Loading